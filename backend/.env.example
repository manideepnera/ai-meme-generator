# =============================================================================
# AI Meme Generator Backend - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to version control!
# =============================================================================

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Application name (optional, has default)
APP_NAME=AI Meme Generator Backend

# Application version (optional, has default)
APP_VERSION=1.0.0

# Debug mode - set to true for development (enables hot reload)
DEBUG=false

# =============================================================================
# LLAMA API SETTINGS (AWS)
# =============================================================================

# REQUIRED: LLaMA API endpoint URL
# This is the URL where your LLaMA model is hosted on AWS.
# 
# Examples:
#   AWS SageMaker:
#     LLAMA_API_URL=https://runtime.sagemaker.us-east-1.amazonaws.com/endpoints/my-llama-endpoint/invocations
#   
#   AWS Lambda + API Gateway:
#     LLAMA_API_URL=https://abc123.execute-api.us-east-1.amazonaws.com/prod/generate
#   
#   Custom EC2 deployment:
#     LLAMA_API_URL=https://my-ec2-instance.compute.amazonaws.com/v1/generate
#
LLAMA_API_URL=

# REQUIRED: LLaMA API authentication key/token
# The format depends on your authentication setup.
# 
# Examples:
#   Bearer token: your-long-api-token-here
#   API Key: your-api-key-here
#
LLAMA_API_KEY=

# Authentication type for LLaMA API
# Options: bearer, api_key, aws_signature, none
# 
# - bearer: Uses "Authorization: Bearer <LLAMA_API_KEY>" header
# - api_key: Uses "x-api-key: <LLAMA_API_KEY>" header (common with API Gateway)
# - aws_signature: Uses AWS Signature V4 (requires AWS credentials below)
# - none: No authentication
#
LLAMA_AUTH_TYPE=bearer

# Timeout for LLaMA API calls (in seconds)
LLAMA_TIMEOUT=60

# =============================================================================
# GOOGLE COLAB SETTINGS
# =============================================================================

# REQUIRED: Google Colab endpoint URL
# This is the URL exposed by your Colab notebook (via ngrok or similar).
# 
# IMPORTANT: Colab URLs are temporary and change on each session restart!
# You'll need to update this each time you restart your Colab notebook.
#
# Examples:
#   ngrok: https://abc123.ngrok.io/generate
#   Cloudflare Tunnel: https://my-colab.trycloudflare.com/generate
#
COLAB_API_URL=

# Optional: Colab API authentication key
# Only needed if you've added authentication to your Colab endpoint.
COLAB_API_KEY=

# Timeout for Colab API calls (in seconds)
# Image generation can take longer, so this should be higher than LLaMA timeout.
COLAB_TIMEOUT=120

# =============================================================================
# GEMINI API (meme concept + caption – used instead of LLaMA for Puter flow)
# =============================================================================
# Get your key at https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your-gemini-api-key
# GEMINI_MODEL=gemini-1.5-flash
# GEMINI_TIMEOUT=60

# =============================================================================
# STABLE DIFFUSION (OpenAI) SETTINGS (OPTIONAL / LEGACY)
# =============================================================================
# The frontend uses Puter.js for AI (no API key required). You do NOT need
# an OpenAI key for normal use.
# When set here, the BACKEND can also generate memes via OpenAI (alternative path).
#
# STABLE_DIFFUSION_API_KEY=
# STABLE_DIFFUSION_CHAT_MODEL=gpt-4o
# STABLE_DIFFUSION_IMAGE_MODEL=dall-e-3
# STABLE_DIFFUSION_TIMEOUT=120

# =============================================================================
# AWS CREDENTIALS (Only if using AWS Signature V4 authentication)
# =============================================================================

# Only required if LLAMA_AUTH_TYPE=aws_signature
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_REGION=us-east-1

# =============================================================================
# CORS SETTINGS
# =============================================================================

# Allowed origins for CORS (comma-separated)
# In production, set this to your actual frontend URL(s).
#
# Examples:
#   Development: http://localhost:3000,http://127.0.0.1:3000
#   Production: https://your-frontend.com,https://www.your-frontend.com
#
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
